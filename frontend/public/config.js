/* Dev / preview default: same-origin relative /api (Vite proxy).
 * Production image regenerates this file at container start from API_BASE_URL. */
window.__AP_CONFIG__ = Object.freeze({ apiBaseUrl: '' });
