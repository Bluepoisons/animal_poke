# 项目长期笔记

## 项目
LBS 动物收集手游（基于 CatchCat 概念改进）。
目录：/Users/bluepoisonss/Desktop/objects/animal_poke

## 项目约定（重要）
- **设计文档唯一来源**：所有游戏设计改进只写进 `游戏开发计划.md`，不产出额外散落文件（不单独生成对辩汇总、评估图等过程文件）。开发计划.md 是唯一最终方案。
- 用户偏好：先聚焦玩法设计本身，暂不考虑商业化效益。
- **API 密钥管理（v1.3 起）**：**所有第三方 Key（腾讯地图/彩云天气/云端 VLM/云端 LLM）只存 Go 后端的 `.env`，客户端不持有、不接触任何第三方 Key**，客户端仅持后端地址 `BACKEND_BASE_URL` + 设备 Token。`.env` 已加入 .gitignore，禁止硬编码。
- **git 分叉处理**：用 rebase（`git pull --rebase`），保持线性历史。

## 技术栈（2026-07-08 确认）
- **前端框架**：已从 Godot 4.7 全面迁移到 **React 18 + Vite 6 + TypeScript 5.6（Web/PWA）**，前后端分离（2026-07-08，v1.4）。
- **AI 推理**：从端侧改为**云端推理**，架构改为**在线优先**（远程 commit dae109c），隐私政策同步修订。
- **后端服务（v1.3 新增）**：**Go 后端（Gin/Echo）作为联网服务总枢纽**，收口所有联网服务（鉴权/同步/反作弊/区域排行/对象存储）+ 全部第三方 API 代理与 AI 推理编排。客户端零第三方 Key、零直连外部服务，只与 Go 后端通信。照片链路：客户端→Go 后端→VLM→后端即时销毁。

## 当前进度（2026-07-10 更新）
开发计划已迭代至玩法深化版，含：
- 核心循环（发现→捕获→收藏→战斗）
- 体力（每小时+10，单次20，上限120→240）/等级/成就
- 天气（影响捕捉难度+宠物状态，不改刷新）
- 区域排行（城市分区）/经济（金币+道具商店+派遣）/状态（感冒/愉悦）
- 第十章「玩法深化」：捕获连击/战斗半自动/宠物养成进化/PVE关卡/社交公会/赛季长线/派遣任务化+居家细化

甲乙方对辩已达成 29 项共识（双货币/永久损伤下线/感冒降温/派遣冷却制/排期10月/合规分工等），对辩汇总过程文件已删除（按"单一文件"约定），共识待后续择机整合进开发计划。

**2026-07-08 照片图鉴+地图系统**：基于 5 张参考图（CatchCat 推文/图鉴网格/投掷教程/详情页/HUNT MAP 手绘地图）改造 CollectScreen 为照片网格图鉴，无物种名，仅编号/日期/地点元信息；新增 MapScreen 手绘地图屏，标记捕捉地点、点击弹出照片浮卡。新增 `ui_collection_map_design.html` 设计稿。测试覆盖：test_ui 46 断言，test_map 15 断言，均通过。

**2026-07-10 后端审计 issue 落地（ji233-Sun）**：关闭 assigned 24 项（#83–#146 子集）。后端具备 APP_ENV/生产禁 Mock、VISION/LLM 双 Provider、JWT 强化、图片上传限制、推理 provenance、版本化迁移、事务同步、批量/游标同步、隐私导出删除、审计 RBAC、商业化订单权益。`go test/race/vet` 全绿。端到端真实捕获主链路仍依赖未分配前端子任务（设备 Token Client、相机、Detect 接入等）。

## UI 风格约定（2026-07-08 确立）
- **视觉风格**：暖色橙色调卡通风。主色 `#FF8C42`（温暖橙）/ 深橙 `#E67300` / 奶油背景 `#FFF8F0` / 深棕文字 `#4A2C1A`。体力黄 `#FFD23F`、金币金 `#FFB300`。
- **字体**：圆体（Baloo 2 + Nunito，中文回退 PingFang SC / 微软雅黑）；前端用系统圆体字体栈（PingFang SC / 微软雅黑 / sans-serif）。
- **组件**：圆角 16px（大）/ 10px（小）；按钮带 4px 实色下沿模拟立体按压感；暖色阴影 rgba(230,115,0,.12)。
- **稀有度边框色不改**：灰/绿/蓝/紫/金（前端 `Rarity` 常量 / 5.1 表硬性约定），保持稀有度视觉辨识度，不随主色调变化。
- **现状**：`frontend/src/index.css` 为暖色橙色调卡通风 CSS 变量主题；UI 设计稿见 `ui_design_m1.html` 与 `ui_collection_map_design.html`。地图系统通过 Go 后端 `/geo/city` 代理腾讯地图逆地理 API（当前 mock，联调期接入）。
